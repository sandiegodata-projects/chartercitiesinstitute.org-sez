#!/bin/bash --login
# The --login ensures the bash configuration is loaded,
# enabling Conda.

#set -euo pipefail
#set -eu

source /opt/conda/etc/profile.d/conda.sh 
#conda activate sez
exec /bin/bash  